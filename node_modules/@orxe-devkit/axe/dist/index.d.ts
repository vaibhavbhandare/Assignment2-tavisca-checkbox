import { toHaveNoViolations } from 'jest-axe';
import defaultConfig from './default.config';
declare function configure(config?: {
    branding: {
        brand: string;
        application: string;
    };
    runOnly: {
        type: string;
        values: string[];
    };
    rules: {};
    resultTypes: string[];
}): (element: any) => Promise<import("axe-core").AxeResults>;
declare let axe: (element: any) => Promise<import("axe-core").AxeResults>;
export { axe, toHaveNoViolations, configure, defaultConfig };
